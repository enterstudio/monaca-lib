general:
  branches:
    only:
      - master
      - stage
machine:
  node:
    version: 6.14.3
checkout:
  post:
    - git fetch --tags
deployment:
  release:
    tag: /[0-9]+(\.[0-9]+)*/
    commands:
      - echo -e "$NPMUSER\n$NPMPASSWORD\n$NPMEMAIL" | npm login
      - npm publish

